Before finishing this task you MUST:

1. Run TypeScript checks.

2. Run npm run build.

3. Fix every compile error.

4. Fix every lint error.

5. Never leave the application in a broken state.

6. If a feature cannot be completed safely,

stop before modifying unrelated files.

7. Never modify database schema unless explicitly requested.

8. Never create placeholder backend code.

9. Every commit must leave the platform fully usable.

Only finish after the build succeeds.